import PlaceholderPage from "./PlaceholderPage";

export default function SubmitFix() {
  return (
    <PlaceholderPage
      title="Submit Fix"
      description="This page will allow security experts and community members to submit fixes and patches for registered systems, including vulnerability description, fix steps, source, and optional contributor information."
    />
  );
}
